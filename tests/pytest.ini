[pytest]
markers =
    smoke: smoke tests
    regression: regression tests
    login: login related tests

addopts = -v --html=reports/report.html --alluredir=reports/allure-results --self-contained-html --tracing retain-on-failure